[build]
  publish = "public"
  functions = "netlify/functions"

# Bonita URL: /api/quotes -> /.netlify/functions/quotes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  node_bundler = "esbuild"
